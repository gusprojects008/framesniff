nome do programa: framesniff: Network Analyzer.

opção para selecionar interface | capture (parar ou recomeçar) | opção para selecionar banda e canal e largura | opção channel hopper (parar ou começar) esse channel hopper pode recebe a configuração de channel hopping de um arquivo json gerado por generate channel hopping config, ou o usuário pode gerar um localmente escolhendo a configuraçao (bandas, timeout do channel hopper, dwell time de cada canal, largura de canal | Traffic | protocol preferences.
input para filtro de display
input para filtro de armazenamento
Visualização de pacote:
A mesma dinâmica do wireshark/termshark.
Um campo selecionado na parte da estrutura do frame parseado, vai destacar na estrutura hexdump montada, e vice-versa.
Navegação pela estrutura do frame parseado [opções: irá haver as opções: Decode as (Para o usuário escolher se quer parsear o payload, e com qual parser), export packets bytes (exportar pacote em pcap, pcapng ou json, se for json o usuário poderá escolher entre o formato légivel ou o simple_format), copy (field, value, description, all visible selected tree fields, all visible fields, as filter, raw)], | hexdump navegavel [opções: show bytes as (hexadecimal, decimal, octal, bits), show text in frame as (ASCII, E8CDIC))

Algumas regras de negócio:
O usuário não poderá mudar de banda, largura ou canal, se o channel hopping estiver ativado.

Falta decidir a estrutura e funcionamento de:
Traffic.
Protocol preferences.
